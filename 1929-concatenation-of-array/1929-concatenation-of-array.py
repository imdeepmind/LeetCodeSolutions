class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        _new = []

        for num in nums:
            _new.append(num)
        
        for num in nums:
            _new.append(num)
        
        return _new